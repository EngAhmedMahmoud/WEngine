<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title= locale.title
    body
        h1= locale.title
        form(action="information",method="post")
            label(for="name")= locale.title
                textarea#des(name="desc", cols="30", rows="10")